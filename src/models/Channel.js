const Base = require('./Base');

/**
* Represents a channel in discordapp
*/
class Channel extends Base {

  constructor(client) {
    super(client);
     console.log(client)
   }


}

module.exports = Channel;
